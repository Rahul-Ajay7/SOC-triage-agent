services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    volumes:
      # Mount the frontend so the API can serve the dashboard at /
      - ./frontend:/frontend:ro
    restart: unless-stopped
